import { Button } from "@/components/ui/button";

/* const delay = (ms) =>
  new Promise((resolve) => {
    setTimeout(() => {
      resolve(true);
    }, ms);
  }); */
const Homepage = async () => {
  // await delay(2000);
  return (
    <>
      <Button>Hello</Button>
    </>
  );
};

export default Homepage;
